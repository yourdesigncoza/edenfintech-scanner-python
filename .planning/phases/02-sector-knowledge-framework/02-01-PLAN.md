---
phase: 02-sector-knowledge-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/methodology/sector-knowledge.schema.json
  - src/edenfintech_scanner_bootstrap/sector.py
  - src/edenfintech_scanner_bootstrap/assets.py
  - tests/test_sector.py
  - tests/fixtures/sector/sample-knowledge.json
  - tests/fixtures/sector/mock-gemini-sector-response.json
  - .gitignore
autonomous: true
requirements:
  - SECT-01
  - SECT-02
  - SECT-04

must_haves:
  truths:
    - "sector-knowledge.schema.json passes validate-assets"
    - "load_sector_knowledge() returns validated sector data from disk"
    - "check_sector_freshness() returns STALE for sectors older than 180 days and FRESH for newer"
    - "check_sector_freshness() returns NOT_HYDRATED for unknown sectors"
    - "Registry tracks hydration timestamps per sector"
    - "data/ directory is gitignored"
  artifacts:
    - path: "assets/methodology/sector-knowledge.schema.json"
      provides: "Sector knowledge JSON Schema with per-sub-sector evidence items"
      contains: "sub_sector_knowledge"
    - path: "src/edenfintech_scanner_bootstrap/sector.py"
      provides: "Sector module with hydrate_sector, load_sector_knowledge, check_sector_freshness"
      exports: ["hydrate_sector", "load_sector_knowledge", "check_sector_freshness"]
    - path: "src/edenfintech_scanner_bootstrap/assets.py"
      provides: "sector_knowledge_schema_path() helper"
      contains: "sector_knowledge_schema_path"
    - path: "tests/test_sector.py"
      provides: "Unit tests for sector module"
      min_lines: 100
    - path: "tests/fixtures/sector/sample-knowledge.json"
      provides: "Valid sector knowledge fixture for tests"
  key_links:
    - from: "src/edenfintech_scanner_bootstrap/sector.py"
      to: "assets/methodology/sector-knowledge.schema.json"
      via: "validate_instance(knowledge_doc, schema) in hydrate_sector and load_sector_knowledge"
      pattern: "validate_instance"
    - from: "src/edenfintech_scanner_bootstrap/sector.py"
      to: "src/edenfintech_scanner_bootstrap/assets.py"
      via: "sector_knowledge_schema_path() import"
      pattern: "sector_knowledge_schema_path"
    - from: "src/edenfintech_scanner_bootstrap/sector.py"
      to: "data/sectors/registry.json"
      via: "_load_registry/_update_registry for staleness tracking"
      pattern: "registry"
---

<objective>
Create the sector knowledge schema, test fixtures, and core sector module with storage, loading, and staleness tracking.

Purpose: Establishes the data contract (schema) and core module that Plan 02 will wire Gemini queries and CLI into. Tests validate schema shape and module behavior using fixtures.
Output: `sector-knowledge.schema.json`, `sector.py` with public API, `assets.py` helper, test suite, fixtures
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sector-knowledge-framework/02-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/edenfintech_scanner_bootstrap/assets.py:
```python
def package_root() -> Path: ...
def assets_root() -> Path: ...
def methodology_root() -> Path: ...
def load_json(path: Path) -> dict: ...
# Pattern: each schema gets a _schema_path() helper, e.g.:
def scan_input_schema_path() -> Path: ...
def gemini_raw_bundle_schema_path() -> Path: ...
```

From src/edenfintech_scanner_bootstrap/schemas.py:
```python
class SchemaValidationError(ValueError): ...
def validate_instance(instance: object, schema: dict, *, root_schema: dict | None = None, path: str = "$") -> None: ...
```

From src/edenfintech_scanner_bootstrap/config.py:
```python
class AppConfig:
    fmp_api_key: str | None
    gemini_api_key: str | None
    openai_api_key: str | None
    def require(self, *fields: str) -> None: ...
def discover_project_root(start: Path | None = None) -> Path | None: ...
def load_config(dotenv_path: Path | None = None) -> AppConfig: ...
```

From src/edenfintech_scanner_bootstrap/gemini.py:
```python
class GeminiClient:
    def __init__(self, api_key: str, *, model: str = DEFAULT_GEMINI_MODEL, transport: GeminiTransport | None = None) -> None: ...
    # transport signature: (url: str, headers: dict, payload: dict) -> dict
    def qualitative_research(self, ticker: str, *, research_question: str, search_scope: str) -> dict: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create sector knowledge schema and test fixtures</name>
  <files>
    assets/methodology/sector-knowledge.schema.json,
    tests/fixtures/sector/sample-knowledge.json,
    tests/fixtures/sector/mock-gemini-sector-response.json,
    src/edenfintech_scanner_bootstrap/assets.py,
    .gitignore
  </files>
  <behavior>
    - Test: valid sample-knowledge.json passes validate_instance against sector-knowledge.schema.json
    - Test: knowledge missing required sub-sector field (e.g. no "kill_factors") is rejected by validate_instance
    - Test: knowledge with empty sub_sectors array is rejected (minItems: 1)
    - Test: evidence_item missing source_url is rejected
    - Test: sector_knowledge_schema_path() returns correct path and file exists
  </behavior>
  <action>
    1. Create `assets/methodology/sector-knowledge.schema.json` using the exact schema from 02-RESEARCH.md "Sector Knowledge Schema Structure" section. The schema has `definitions` for `evidence_item` and `sub_sector_knowledge` with 8 required knowledge categories each containing arrays of evidence items with claim/source_title/source_url.

    2. Add `sector_knowledge_schema_path()` to `assets.py` following the existing pattern:
       ```python
       def sector_knowledge_schema_path() -> Path:
           return methodology_root() / "sector-knowledge.schema.json"
       ```

    3. Create `tests/fixtures/sector/sample-knowledge.json` — a complete valid sector knowledge document for "Consumer Defensive" with 1 sub-sector ("Household Products") and 1 evidence item per category. Use realistic claim text and placeholder URLs. Include all top-level required fields: sector_name, sector_slug, hydrated_at (ISO datetime), model, sub_sectors.

    4. Create `tests/fixtures/sector/mock-gemini-sector-response.json` — a mock Gemini API response for a single category query, matching the `_sector_response_schema` shape from research (object with "items" array of evidence_item objects).

    5. Add `data/` to `.gitignore` (after the existing `runs/` entry).

    6. Write tests in `tests/test_sector.py` classes `TestSectorSchema` covering the behaviors above. Use `from edenfintech_scanner_bootstrap.assets import sector_knowledge_schema_path, load_json` and `from edenfintech_scanner_bootstrap.schemas import validate_instance, SchemaValidationError`.
  </action>
  <verify>
    <automated>cd /home/laudes/zoot/projects/edenfintech-scanner-python && source .venv/bin/activate && python -m unittest tests.test_sector.TestSectorSchema -v && python -m edenfintech_scanner_bootstrap.cli validate-assets</automated>
  </verify>
  <done>Schema file exists and validates sample fixture. assets.py has sector_knowledge_schema_path(). validate-assets passes (schema is well-formed). data/ is in .gitignore.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sector module core — storage, loading, freshness, and hydration skeleton</name>
  <files>
    src/edenfintech_scanner_bootstrap/sector.py,
    tests/test_sector.py
  </files>
  <behavior>
    - Test: hydrate_sector() with mock GeminiClient writes knowledge.json to data/sectors/{slug}/knowledge.json and validates against schema
    - Test: hydrate_sector() updates registry.json with sector_name, hydrated_at, sub_sectors, knowledge_path
    - Test: load_sector_knowledge("Consumer Defensive") returns dict that passes schema validation (from fixture on disk)
    - Test: load_sector_knowledge() raises for unknown sector
    - Test: check_sector_freshness() returns {"status": "FRESH", "stale": False} for sector hydrated today
    - Test: check_sector_freshness() returns {"status": "STALE", "stale": True} for sector hydrated 200 days ago
    - Test: check_sector_freshness() returns {"status": "NOT_HYDRATED", "stale": True} for unknown sector
    - Test: _slugify("Consumer Defensive") returns "consumer-defensive"
    - Test: 8 Gemini queries are made per sub-sector (mock transport counts calls)
  </behavior>
  <action>
    Create `src/edenfintech_scanner_bootstrap/sector.py` with the full implementation following the research sketches:

    **Constants:** STALENESS_DAYS = 180, SECTOR_DATA_DIR = "data/sectors", REGISTRY_FILENAME = "registry.json", KNOWLEDGE_CATEGORIES list of 8 categories.

    **Private helpers:**
    - `_slugify(name: str) -> str` — `re.sub(r"[^a-z0-9]+", "-", name.lower()).strip("-")`
    - `_sector_dir(project_root, sector_slug) -> Path`
    - `_registry_path(project_root) -> Path`
    - `_load_registry(project_root) -> dict` — reads registry.json or returns `{"sectors": {}}`
    - `_update_registry(project_root, sector_name, sector_slug, sub_sectors)` — writes updated registry.json
    - `_sector_query_prompt(sub_sector, category) -> str` — prompt templates for 8 categories (from research Pattern 1)
    - `_sector_response_schema(category) -> dict` — JSON Schema for single category response (evidence_item array)
    - `_hydrate_sub_sector(sub_sector, sector_name, client) -> dict` — runs 8 Gemini queries for one sub-sector, adds time.sleep(2) between queries to avoid rate limits. Each query uses the GeminiClient's transport directly (build the same payload structure as qualitative_research but with sector-specific prompts and response schema). Returns a sub_sector_knowledge object.

    **Public API:**
    - `hydrate_sector(sector_name, *, sub_sectors=None, client=None, config=None, project_root=None) -> dict` — follows exact signature from research. If sub_sectors not provided, accept them as required (FMP discovery is optional; for now raise ValueError if not provided, with a TODO for FMP screener discovery in Phase 6). Validates final doc against schema. Writes to disk. Updates registry.
    - `load_sector_knowledge(sector_name, *, project_root=None) -> dict` — reads knowledge.json from disk, validates against schema, returns dict. Raises FileNotFoundError if not hydrated.
    - `check_sector_freshness(sector_name, *, project_root=None) -> dict` — returns status dict per research.

    **Test additions** to `tests/test_sector.py`: Add classes `TestHydrateSector`, `TestLoadSectorKnowledge`, `TestSectorFreshness`, `TestGeminiSectorQueries`. Use `unittest.mock.patch` to mock GeminiClient transport. Use `tempfile.TemporaryDirectory` for project_root in tests. Pre-write sample-knowledge.json fixture to temp dirs for load tests. For hydration tests, mock the transport to return the mock-gemini-sector-response.json fixture.
  </action>
  <verify>
    <automated>cd /home/laudes/zoot/projects/edenfintech-scanner-python && source .venv/bin/activate && python -m unittest tests.test_sector -v</automated>
  </verify>
  <done>sector.py exists with hydrate_sector, load_sector_knowledge, check_sector_freshness. All unit tests pass. Hydration writes valid knowledge.json. Registry tracks hydration dates. Staleness detection works at 180-day threshold.</done>
</task>

</tasks>

<verification>
- `python -m unittest tests.test_sector -v` — all sector tests pass
- `python -m edenfintech_scanner_bootstrap.cli validate-assets` — schema validates
- `python -m unittest discover -s tests -v` — no regressions in existing tests
</verification>

<success_criteria>
- sector-knowledge.schema.json exists and passes asset validation
- sector.py module has 3 public functions: hydrate_sector, load_sector_knowledge, check_sector_freshness
- Storage layer writes to data/sectors/{slug}/knowledge.json with registry.json tracking
- Staleness detection works (180-day threshold, NOT_HYDRATED for unknown sectors)
- All tests pass including existing regression suite
</success_criteria>

<output>
After completion, create `.planning/phases/02-sector-knowledge-framework/02-01-SUMMARY.md`
</output>
