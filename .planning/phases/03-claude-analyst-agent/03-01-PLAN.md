---
phase: 03-claude-analyst-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/edenfintech_scanner_bootstrap/structured_analysis.py
  - src/edenfintech_scanner_bootstrap/config.py
  - src/edenfintech_scanner_bootstrap/analyst.py
  - src/edenfintech_scanner_bootstrap/live_scan.py
  - src/edenfintech_scanner_bootstrap/cli.py
  - assets/methodology/structured-analysis.schema.json
  - .env.example
  - tests/test_analyst.py
  - tests/fixtures/analyst/llm-response-fixture.json
autonomous: true
requirements: [AGNT-01, AGNT-02, AGNT-03, AGNT-04, AGNT-05]

must_haves:
  truths:
    - "Given a fixture raw bundle, the analyst overlay has no __REQUIRED__ placeholders"
    - "Every field_provenance entry has non-empty review_note citing a named source"
    - "All provenance entries carry LLM_DRAFT status"
    - "Overlay passes validate_structured_analysis()"
    - "worst_case appears before base_case in raw response text"
  artifacts:
    - path: "src/edenfintech_scanner_bootstrap/analyst.py"
      provides: "ClaudeAnalystClient class, schema builder, evidence extraction, generate_llm_analysis_draft"
      min_lines: 80
    - path: "tests/test_analyst.py"
      provides: "Tests for all AGNT requirements including raw-text ordering"
      min_lines: 50
    - path: "tests/fixtures/analyst/llm-response-fixture.json"
      provides: "Fixture LLM response payload for transport injection"
      min_lines: 20
  key_links:
    - from: "src/edenfintech_scanner_bootstrap/analyst.py"
      to: "assets/methodology/structured-analysis.schema.json"
      via: "_build_candidate_output_schema loads and subsets schema"
      pattern: "structured_analysis_schema_path"
    - from: "src/edenfintech_scanner_bootstrap/structured_analysis.py"
      to: "DRAFT_PROVENANCE_STATUSES"
      via: "finalize_structured_analysis checks both MACHINE_DRAFT and LLM_DRAFT"
      pattern: "LLM_DRAFT"
    - from: "src/edenfintech_scanner_bootstrap/live_scan.py"
      to: "src/edenfintech_scanner_bootstrap/analyst.py"
      via: "--use-analyst flag selects generate_llm_analysis_draft instead of generate_structured_analysis_draft"
      pattern: "generate_llm_analysis_draft"
    - from: "src/edenfintech_scanner_bootstrap/cli.py"
      to: "src/edenfintech_scanner_bootstrap/analyst.py"
      via: "build-review-package --use-analyst option wired through CLI"
      pattern: "use.analyst"
---

<objective>
Complete Claude analyst agent: LLM_DRAFT provenance, config, constrained decoding schema builder, analyst module, test fixtures, and CLI/workflow wiring.

Purpose: Delivers a fully invocable analyst agent that an operator can use via `build-review-package --use-analyst` to generate LLM-drafted structured analysis overlays instead of deterministic machine drafts.

Output: analyst.py module with ClaudeAnalystClient, updated provenance system, fixture-backed tests, and CLI wiring into live_scan.py.
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-claude-analyst-agent/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/edenfintech_scanner_bootstrap/structured_analysis.py:
```python
REQUIRED_PROVENANCE_FIELDS = [
    "screening_inputs.industry_understandable",
    "screening_inputs.industry_in_secular_decline",
    # ... 23 total fields
    "epistemic_inputs.q5_macro",
]
FINAL_PROVENANCE_STATUSES = {"HUMAN_EDITED", "HUMAN_CONFIRMED"}

def _candidate_evidence_context(raw_candidate: dict) -> dict: ...
def _fingerprint(value: object) -> str: ...
def _raw_bundle_fingerprint(raw_bundle: dict) -> str: ...
def validate_structured_analysis(payload: dict) -> None: ...
def finalize_structured_analysis(payload, *, reviewer, final_status="HUMAN_CONFIRMED", note=None) -> dict: ...
```
Finalize currently only transitions status=="MACHINE_DRAFT" (line 707). Must extend to also transition "LLM_DRAFT".

From src/edenfintech_scanner_bootstrap/config.py:
```python
@dataclass(frozen=True)
class AppConfig:
    fmp_api_key: str | None
    gemini_api_key: str | None
    openai_api_key: str | None
    codex_judge_model: str
```

From src/edenfintech_scanner_bootstrap/assets.py:
```python
def structured_analysis_schema_path() -> Path: ...
def load_json(path: Path) -> dict: ...
```

From src/edenfintech_scanner_bootstrap/gemini.py (transport pattern):
```python
GeminiTransport = Callable[[str, dict[str, str], dict], dict]
class GeminiClient:
    def __init__(self, api_key, *, model="...", transport=None):
        self.transport = transport or _default_transport
```

From src/edenfintech_scanner_bootstrap/live_scan.py (current draft generation call):
```python
from .field_generation import generate_structured_analysis_draft
# line ~81:
structured_draft = generate_structured_analysis_draft(merged_bundle)
```

From src/edenfintech_scanner_bootstrap/cli.py (build-review-package command):
```python
# build-review-package subparser exists, calls into live_scan flow
# generate-structured-analysis-draft subparser at line ~444
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LLM_DRAFT provenance status and config additions</name>
  <files>
    src/edenfintech_scanner_bootstrap/structured_analysis.py,
    src/edenfintech_scanner_bootstrap/config.py,
    assets/methodology/structured-analysis.schema.json,
    .env.example
  </files>
  <behavior>
    - Test: LLM_DRAFT is accepted in schema validation for field_provenance.status enum
    - Test: finalize_structured_analysis transitions LLM_DRAFT entries to final_status (same as MACHINE_DRAFT behavior)
    - Test: finalize_structured_analysis refuses LLM_DRAFT without review_note
    - Test: _validate_provenance_coverage rejects LLM_DRAFT when allow_machine_draft=False (same as MACHINE_DRAFT)
    - Test: AppConfig has anthropic_api_key and analyst_model fields
    - Test: load_config reads ANTHROPIC_API_KEY and ANALYST_MODEL env vars
  </behavior>
  <action>
    1. In `structured-analysis.schema.json`: add "LLM_DRAFT" to the field_provenance status enum array (alongside MACHINE_DRAFT, HUMAN_EDITED, HUMAN_CONFIRMED).

    2. In `structured_analysis.py`:
       - Add `DRAFT_PROVENANCE_STATUSES = {"MACHINE_DRAFT", "LLM_DRAFT"}` constant after FINAL_PROVENANCE_STATUSES.
       - In `_validate_provenance_coverage`: change `status == "MACHINE_DRAFT"` check to `status in DRAFT_PROVENANCE_STATUSES` so LLM_DRAFT is treated identically.
       - In `finalize_structured_analysis`: change `item.get("status") == "MACHINE_DRAFT"` (line ~707) to `item.get("status") in DRAFT_PROVENANCE_STATUSES` so LLM_DRAFT entries get transitioned too.
       - Add enriched Codex field paths to REQUIRED_PROVENANCE_FIELDS (these come from Phase 1 schema enrichments, but the provenance list needs updating now):
         "analysis_inputs.catalyst_stack", "analysis_inputs.invalidation_triggers", "analysis_inputs.decision_memo", "analysis_inputs.issues_and_fixes_detail", "analysis_inputs.setup_pattern", "analysis_inputs.stretch_case_assumptions"
         NOTE: Only add these if the fields actually exist in the current schema. Check the schema first. If they don't exist yet (Phase 1 Plan 02 not executed), skip this sub-step and add a comment noting they should be added when Phase 1 completes.

    3. In `config.py`: add `anthropic_api_key: str | None` and `analyst_model: str` to AppConfig dataclass. In `load_config()`, read `ANTHROPIC_API_KEY` env var and `ANALYST_MODEL` with default `"claude-sonnet-4-5-20250514"`.

    4. In `.env.example`: add `ANTHROPIC_API_KEY=` and `ANALYST_MODEL=claude-sonnet-4-5-20250514`.
  </action>
  <verify>
    <automated>cd /home/laudes/zoot/projects/edenfintech-scanner-python && python -m unittest discover -s tests -v 2>&1 | tail -5</automated>
  </verify>
  <done>LLM_DRAFT accepted by schema, finalize transitions both MACHINE_DRAFT and LLM_DRAFT, config exposes Anthropic settings. All existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Analyst module with schema builder, test fixtures, and raw-text ordering validation</name>
  <files>
    src/edenfintech_scanner_bootstrap/analyst.py,
    tests/test_analyst.py,
    tests/fixtures/analyst/llm-response-fixture.json
  </files>
  <behavior>
    - Test: ClaudeAnalystClient can be instantiated with a mock transport
    - Test: _build_candidate_output_schema returns a valid JSON schema with additionalProperties:false and no minLength/minimum/maximum/minItems
    - Test: _strip_unsupported_constraints removes minLength, minimum, maximum, minItems, maxItems, maxLength
    - Test: _build_candidate_output_schema includes screening_inputs, analysis_inputs, epistemic_inputs, field_provenance as required
    - Test: field_provenance status enum in output schema is exactly ["LLM_DRAFT"]
    - Test: _extract_evidence_snippets extracts source_title values from raw candidate evidence
    - Test: generate_llm_analysis_draft with fixture transport produces overlay that passes validate_structured_analysis
    - Test: all provenance entries in fixture output have status LLM_DRAFT
    - Test: all provenance entries have non-empty review_note
    - Test: no __REQUIRED__ placeholders remain in output
    - Test: worst_case appears before base_case in raw response TEXT (capture raw string from fixture transport BEFORE json.loads, assert substring position)
    - Test: bear thesis appears before bull thesis in raw response TEXT (same raw string position check)
  </behavior>
  <action>
    1. Create `tests/fixtures/analyst/llm-response-fixture.json`: A realistic LLM response payload for a single candidate (use a fictional ticker like "ACME"). Must include screening_inputs, analysis_inputs (with all standard + enriched Codex fields if schema supports them), epistemic_inputs, and field_provenance with LLM_DRAFT status and evidence-citing review_notes. Ensure worst_case_assumptions appears before base_case_assumptions in the JSON text, and bear thesis before bull in thesis_summary. This fixture is what the mock transport returns.

    2. Create `src/edenfintech_scanner_bootstrap/analyst.py`:
       - `AnalystTransport = Callable[[dict], dict]` type alias
       - `ClaudeAnalystClient.__init__(self, api_key, *, model="claude-sonnet-4-5-20250514", transport=None)` -- stores api_key, model, transport (or _default_transport)
       - `_default_transport(self, request_payload)` -- imports anthropic, creates client, calls messages.create with output_config format, returns {"text": content[0].text, "stop_reason": stop_reason}
       - `_strip_unsupported_constraints(schema)` -- recursively removes minLength, maxLength, minimum, maximum, minItems, maxItems; adds additionalProperties:false to objects. Handles properties, items, definitions/$defs recursively.
       - `_build_candidate_output_schema()` -- loads structured-analysis.schema.json, extracts candidate-level fields (screening_inputs, analysis_inputs, epistemic_inputs, field_provenance), strips constraints, sets provenance status enum to ["LLM_DRAFT"] only. Returns schema dict.
       - `_extract_evidence_snippets(raw_candidate)` -- collects all source_title values from evidence arrays in the raw candidate. Returns set of valid source titles for post-validation.
       - `_build_system_prompt(sector_knowledge)` -- returns the system prompt string with methodology rules from strategy-rules.md, ordering discipline (worst-case-first, bear-first), evidence citation rules, and provenance rules. Include sector knowledge context if provided.
       - `_build_user_prompt(raw_candidate, evidence_context, evidence_snippets)` -- formats the raw candidate data for the user message.
       - `analyze(self, raw_candidate, *, sector_knowledge=None)` -- builds prompts, constructs request payload with output_config json_schema, calls transport, parses JSON response. IMPORTANT: store raw response text in self._last_raw_response before json.loads() so _post_validate can check ordering.
       - `generate_llm_analysis_draft(raw_bundle, *, client, sector_knowledge=None)` -- iterates raw_candidates, calls client.analyze per candidate, wraps with envelope (same structure as field_generation.py), calls validate_structured_analysis, returns complete overlay.
       - `_post_validate(candidate_output, raw_candidate, raw_response_text)` -- checks no __REQUIRED__ placeholders, every provenance has review_note, review_notes reference valid source_titles from evidence. Also checks worst_case substring position < base_case position in raw_response_text, and bear < bull position.

    3. Create `tests/test_analyst.py`:
       - `TestSchemaBuilder` class: tests for _strip_unsupported_constraints and _build_candidate_output_schema
       - `TestClaudeAnalystClient` class: tests for instantiation, analyze with mock transport
       - `TestAnalystAgent` class: tests for generate_llm_analysis_draft using fixture transport, covering AGNT-01 (placeholders filled), AGNT-02 (LLM_DRAFT status), AGNT-03 (review_notes with citations), AGNT-04 (ordering), AGNT-05 (schema validation)
       - AGNT-04 ordering tests (test_worst_case_before_base_case, test_bear_before_bull): The fixture transport must return a dict with a "text" key containing the raw JSON string. The test must capture this raw string BEFORE it is parsed by json.loads() and assert that the substring "worst_case" appears at a lower index than "base_case" in that raw text, and "bear" before "bull". This validates the _post_validate ordering logic path, not just fixture authoring. Specifically: load the fixture file as a raw string (Path.read_text()), find substring positions, and assert ordering. Then separately verify that _post_validate is called with this raw text and does not raise.
       - Use a fixture transport function that loads llm-response-fixture.json and returns {"text": raw_string, "stop_reason": "end_turn"}
       - Build a minimal raw_bundle fixture inline or load from tests/fixtures/raw/ if suitable fixture exists
  </action>
  <verify>
    <automated>cd /home/laudes/zoot/projects/edenfintech-scanner-python && python -m unittest tests.test_analyst -v 2>&1 | tail -20</automated>
  </verify>
  <done>analyst.py module exists with ClaudeAnalystClient, schema builder, evidence extraction, generate_llm_analysis_draft, and _post_validate with raw-text ordering checks. All test_analyst tests pass including raw-text position assertions for AGNT-04.</done>
</task>

<task type="auto">
  <name>Task 3: Wire analyst into live_scan.py and CLI</name>
  <files>
    src/edenfintech_scanner_bootstrap/live_scan.py,
    src/edenfintech_scanner_bootstrap/cli.py
  </files>
  <action>
    1. In `live_scan.py`:
       - Add import: `from .analyst import generate_llm_analysis_draft, ClaudeAnalystClient`
       - Add import: `from .config import load_config`
       - Modify the function that calls `generate_structured_analysis_draft(merged_bundle)` (around line 81) to accept an optional `use_analyst: bool = False` parameter (or add it to the enclosing function signature).
       - When `use_analyst=True`: instantiate `ClaudeAnalystClient(config.anthropic_api_key, model=config.analyst_model)`, call `generate_llm_analysis_draft(merged_bundle, client=client)` instead of `generate_structured_analysis_draft(merged_bundle)`.
       - When `use_analyst=False` (default): keep existing behavior unchanged.

    2. In `cli.py`:
       - Add `--use-analyst` flag to the `build-review-package` subparser (store_true, default False). Help text: "Use Claude analyst agent instead of deterministic machine draft."
       - Pass the flag value through to the live_scan function so it reaches the conditional in step 1.
       - Also add a standalone `generate-llm-analysis-draft` subcommand (mirrors `generate-structured-analysis-draft`) that accepts `raw_bundle_path` and `--json-out`, loads config, instantiates ClaudeAnalystClient, calls generate_llm_analysis_draft, and writes the result.

    3. Verify no import cycles: analyst.py imports from structured_analysis.py and config.py; live_scan.py imports from analyst.py. This is a clean DAG.
  </action>
  <verify>
    <automated>cd /home/laudes/zoot/projects/edenfintech-scanner-python && python -c "from edenfintech_scanner_bootstrap.live_scan import build_review_package; print('import OK')" && python -m edenfintech_scanner_bootstrap.cli --help 2>&1 | grep -E "use-analyst|generate-llm"</automated>
  </verify>
  <done>build-review-package --use-analyst flag is available in CLI help. generate-llm-analysis-draft subcommand exists. Importing live_scan succeeds without circular import errors. An operator can now invoke the analyst agent from the standard workflow.</done>
</task>

</tasks>

<verification>
1. All existing tests pass: `python -m unittest discover -s tests -v`
2. Asset validation passes: `python -m edenfintech_scanner_bootstrap.cli validate-assets`
3. All analyst tests pass: `python -m unittest tests.test_analyst -v`
4. Schema includes LLM_DRAFT in provenance status enum
5. Config includes anthropic_api_key and analyst_model
6. CLI shows --use-analyst flag: `python -m edenfintech_scanner_bootstrap.cli build-review-package --help | grep use-analyst`
7. CLI shows generate-llm-analysis-draft command: `python -m edenfintech_scanner_bootstrap.cli --help | grep generate-llm`
</verification>

<success_criteria>
- LLM_DRAFT provenance status fully integrated into schema + structured_analysis.py lifecycle
- ClaudeAnalystClient instantiable with mock transport
- Schema builder produces valid constrained decoding schema
- generate_llm_analysis_draft produces overlay passing validate_structured_analysis using fixture transport
- AGNT-04 ordering verified via raw response text position checks (not just fixture JSON structure)
- Analyst wired into live_scan.py via use_analyst flag and accessible via CLI
- All 12+ analyst tests green
- Zero regression in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-analyst-agent/03-01-SUMMARY.md`
</output>
