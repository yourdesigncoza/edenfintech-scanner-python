---
phase: 04-review-agents
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/edenfintech_scanner_bootstrap/validator.py
  - tests/test_validator.py
  - tests/fixtures/validator/llm-response-fixture.json
autonomous: true
requirements: [VALD-01, VALD-02, VALD-03]

must_haves:
  truths:
    - "Validator answers 5 red-team questions as structured output with challenge, evidence, and severity per question"
    - "Deterministic contradiction detection finds revenue/FCF/share discrepancies between analyst overlay and raw FMP data"
    - "Contradiction detection ignores within-threshold differences (revenue <10%, FCF <5pp, shares <5%)"
    - "Validator can REJECT with specific objections array or APPROVE with empty objections"
    - "Validator does NOT see pipeline scores, rankings, or post-scoring data"
  artifacts:
    - path: "src/edenfintech_scanner_bootstrap/validator.py"
      provides: "detect_contradictions(), RedTeamValidatorClient, validate_overlay()"
      min_lines: 100
    - path: "tests/test_validator.py"
      provides: "Tests covering VALD-01 through VALD-03 including contradiction thresholds"
      min_lines: 60
    - path: "tests/fixtures/validator/llm-response-fixture.json"
      provides: "Fixture LLM response for transport injection"
      min_lines: 10
  key_links:
    - from: "src/edenfintech_scanner_bootstrap/validator.py"
      to: "detect_contradictions"
      via: "Deterministic contradictions fed into LLM validator context"
      pattern: "detect_contradictions"
    - from: "src/edenfintech_scanner_bootstrap/validator.py"
      to: "assets/contracts/codex_final_judge.json"
      via: "APPROVE/REJECT verdict pattern mirrors judge contract"
      pattern: "APPROVE|REJECT"
---

<objective>
Build the red-team validator agent with deterministic contradiction detection and LLM-powered adversarial questioning, producing APPROVE/REJECT verdicts with specific objections.

Purpose: Adversarial challenge layer that cross-checks analyst assumptions against raw FMP data (deterministically) and probes thesis quality via 5 red-team questions (LLM). This is the second review agent required before overlay finalization.

Output: validator.py module with contradiction detection + LLM validator, fixture-backed tests, and validator LLM response fixture.
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-review-agents/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From assets/contracts/codex_final_judge.json (verdict pattern):
```json
{
  "enums": {
    "verdict": ["APPROVE", "REVISE"],
    "target_stage": ["screening", "cluster_analysis", "epistemic_review", "report_assembly", "approve"]
  }
}
```
Validator uses a similar pattern but with verdict enum ["APPROVE", "REJECT"] and objections array instead of reroute.

From src/edenfintech_scanner_bootstrap/judge.py (transport pattern):
```python
JudgeTransport = Callable[[dict, AppConfig], dict]
def codex_judge(report, execution_log, *, config=None, transport=None) -> dict:
    # Transport-injectable pattern: tests pass mock transport, production uses HTTP
```

From src/edenfintech_scanner_bootstrap/config.py:
```python
@dataclass(frozen=True)
class AppConfig:
    anthropic_api_key: str | None = None
    analyst_model: str = "claude-sonnet-4-5-20250514"
```

From tests/fixtures/raw/merged_candidate_bundle.json:
```
# Raw candidate bundle shape with fmp_context.derived containing:
# latest_revenue_b, trough_revenue_b, latest_fcf_margin_pct, diluted_shares_m
# These are the FMP-derived fields contradiction detection compares against
```

From the analyst overlay shape (analysis_inputs):
```python
# base_case_assumptions: {revenue_b, fcf_margin_pct, multiple, shares_m, years}
# worst_case_assumptions: same shape
# stretch_case_assumptions: same shape
# catalyst_stack, key_risks, thesis_summary, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic contradiction detection</name>
  <files>
    src/edenfintech_scanner_bootstrap/validator.py,
    tests/test_validator.py
  </files>
  <behavior>
    - Test: detect_contradictions finds HIGH severity when base_case revenue > 1.5x FMP latest revenue
    - Test: detect_contradictions finds MEDIUM severity when FCF margin differs by 5-10pp from FMP
    - Test: detect_contradictions finds HIGH severity when FCF margin differs by >10pp from FMP
    - Test: detect_contradictions returns empty list when all claims within thresholds (revenue <10%, FCF <5pp)
    - Test: detect_contradictions flags revenue growth direction contradiction (analyst claims growth, FMP shows decline)
    - Test: detect_contradictions flags share count discrepancy when >5% off FMP diluted shares
    - Test: each contradiction dict has field, claim, actual, severity keys
  </behavior>
  <action>
    Create `validator.py` with:

    1. **detect_contradictions(overlay_candidate: dict, raw_candidate: dict) -> list[dict]**: Pure Python, no LLM. Per research Pattern 3. Compares analyst overlay analysis_inputs against raw_candidate fmp_context.derived:

       Revenue check: base_case_assumptions.revenue_b vs derived.latest_revenue_b. Flag HIGH if gap > 50%, MEDIUM if gap > 10%.

       FCF margin check: base_case_assumptions.fcf_margin_pct vs derived.latest_fcf_margin_pct. Flag HIGH if gap > 10pp, MEDIUM if gap > 5pp.

       Revenue direction check: If derived shows latest_revenue_b < trough_revenue_b (declining), flag if analyst does not acknowledge decline in margin_trend_gate or thesis.

       Share count check: base_case_assumptions.shares_m vs derived.diluted_shares_m. Flag MEDIUM if gap > 5%.

       Each contradiction returns: {"field": str, "claim": str, "actual": str, "severity": "HIGH"|"MEDIUM"|"LOW"}.

       Handle missing fields gracefully -- skip checks where data is absent rather than erroring.

    Create `tests/test_validator.py` with:
    - TestContradictionDetection: test each comparison (revenue, FCF, direction, shares), test within-threshold no-flag, test missing field graceful skip.
  </action>
  <verify>
    <automated>cd /home/laudes/zoot/projects/edenfintech-scanner-python && python -m unittest tests.test_validator.TestContradictionDetection -v 2>&1 | tail -15</automated>
  </verify>
  <done>detect_contradictions compares 4 analyst claims against FMP data with appropriate thresholds. Returns structured contradiction list. Handles missing fields gracefully. All contradiction tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RedTeamValidatorClient with 5 questions, APPROVE/REJECT verdict, and validate_overlay flow</name>
  <files>
    src/edenfintech_scanner_bootstrap/validator.py,
    tests/test_validator.py,
    tests/fixtures/validator/llm-response-fixture.json
  </files>
  <behavior>
    - Test: RedTeamValidatorClient instantiates with mock transport
    - Test: validate_overlay with fixture transport returns dict with verdict, questions, objections, contradictions keys
    - Test: fixture produces 5 questions each with question_id, challenge, evidence, severity
    - Test: REJECT verdict includes non-empty objections array
    - Test: APPROVE verdict has empty objections array (separate fixture or modified fixture)
    - Test: contradictions from detect_contradictions are included in validator context (verify they appear in request payload)
    - Test: validator does NOT receive pipeline scores or rankings in request payload
  </behavior>
  <action>
    1. Create `tests/fixtures/validator/llm-response-fixture.json`: Realistic LLM response with verdict "REJECT", 5 red-team questions (each with question_id like "bull_falsifiability", "worst_case_completeness", "catalyst_plausibility", "competitive_durability", "management_credibility"), challenge text, evidence, severity (HIGH/MEDIUM/LOW), and objections array with 1-2 specific objection strings.

    2. Add to `validator.py`:

       - **RED_TEAM_QUESTIONS**: List of 5 question templates that probe: (1) bull_falsifiability -- "What specific evidence would falsify the bull case?", (2) worst_case_completeness -- "Does the worst case capture the actual downside scenario?", (3) catalyst_plausibility -- "Are the stated catalysts realistic within the timeline?", (4) competitive_durability -- "Can the competitive position survive disruption?", (5) management_credibility -- "Is management's track record consistent with execution claims?"

       - **VALIDATOR_OUTPUT_SCHEMA**: JSON schema for constrained decoding. Object with verdict (enum APPROVE/REJECT), questions (array of 5 objects with question_id/challenge/evidence/severity), objections (array of strings). additionalProperties: false.

       - **ValidatorTransport = Callable[[dict], dict]**: Type alias.

       - **RedTeamValidatorClient**:
         - `__init__(self, api_key: str, *, model: str = "claude-sonnet-4-5-20250514", transport: ValidatorTransport | None = None)`
         - `_default_transport(self, request_payload: dict) -> dict`: Calls Anthropic API with constrained decoding. Returns {"text": response_text, "stop_reason": stop_reason}.
         - `validate(self, overlay_candidate: dict, raw_candidate: dict, contradictions: list[dict]) -> dict`: Builds system prompt with red-team question templates and adversarial stance. Builds user prompt with overlay analysis_inputs (NOT scores/rankings), raw FMP context, and pre-computed contradictions. Calls transport. Parses JSON. Returns validated result.

       - **validate_overlay(overlay_candidate: dict, raw_candidate: dict, *, client: RedTeamValidatorClient | None = None) -> dict**: Top-level function. Runs detect_contradictions first (per research recommendation: deterministic BEFORE LLM). Instantiates default client if none. Calls client.validate with contradictions included. Returns enriched result with verdict, questions, objections, contradictions keys.

       - Important: The user prompt must NOT include any of: decision_score, total_score, ranking, effective_probability, pipeline output, scan report data. Only overlay analysis_inputs + raw FMP data + contradictions.

    3. Update `tests/test_validator.py`:
       - TestRedTeamValidator: fixture transport, verify 5 questions, REJECT with objections, APPROVE with empty objections (construct a second inline fixture for APPROVE case).
       - TestValidateOverlayFlow: full validate_overlay() call, verify contradictions run first and appear in output, verify request payload excludes scores.
  </action>
  <verify>
    <automated>cd /home/laudes/zoot/projects/edenfintech-scanner-python && python -m unittest tests.test_validator -v 2>&1 | tail -20</automated>
  </verify>
  <done>RedTeamValidatorClient answers 5 red-team questions with structured output. REJECT includes objections, APPROVE has empty objections. Contradictions run deterministically first and feed into LLM context. Validator provably excludes pipeline scores from its input. All tests pass.</done>
</task>

</tasks>

<verification>
1. All existing tests pass: `python -m unittest discover -s tests -v`
2. Asset validation passes: `python -m edenfintech_scanner_bootstrap.cli validate-assets`
3. All validator tests pass: `python -m unittest tests.test_validator -v`
4. Contradiction detection tested with thresholds (revenue 10%, FCF 5pp, shares 5%)
5. Validator request payload does not contain scores, rankings, or pipeline output
6. APPROVE and REJECT paths both tested
</verification>

<success_criteria>
- Deterministic contradiction detection compares 4 analyst fields against FMP data with thresholds (VALD-02)
- 5 red-team questions answered with challenge/evidence/severity (VALD-01)
- REJECT verdict carries specific objections, APPROVE carries empty objections (VALD-03)
- Contradictions feed into LLM context (deterministic first, then LLM)
- Validator provably blind to pipeline scores and rankings
- Zero regression in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-review-agents/04-02-SUMMARY.md`
</output>
