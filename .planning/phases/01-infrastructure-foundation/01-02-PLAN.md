---
phase: 01-infrastructure-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/methodology/scan-input.schema.json
  - assets/methodology/structured-analysis.schema.json
  - src/edenfintech_scanner_bootstrap/pipeline.py
  - tests/test_schema_enrichment.py
  - tests/test_scan_pipeline.py
  - tests/fixtures/raw/merged_candidate_bundle.json
  - tests/fixtures/raw/ranked_candidate_bundle.json
  - tests/fixtures/generated/merged_candidate_draft_overlay.json
  - assets/fixtures/regression/2026-03-07-cps-aap-dorm-pypl-scan-report-v5.json
  - assets/fixtures/regression/pending-human-review-exception.json
  - assets/fixtures/regression/manifest.json
autonomous: true
requirements: [SCHM-01, SCHM-02, SCHM-03, SCHM-04, SCHM-05, SCHM-06, SCHM-07, SCHM-08]

must_haves:
  truths:
    - "validate-assets passes with enriched schemas containing catalyst_stack, invalidation_triggers, decision_memo, issues_and_fixes, setup_pattern, stretch_case"
    - "Schema validates catalyst_stack entries with HARD/MEDIUM/SOFT type, description, timeline"
    - "Schema validates issues_and_fixes as array with evidence_status enum"
    - "Pipeline rejects scan-input with zero HARD/MEDIUM catalyst_stack entries"
    - "Pipeline rejects when all issues_and_fixes are ANNOUNCED_ONLY"
    - "All existing tests and regression fixtures still pass"
  artifacts:
    - path: "assets/methodology/scan-input.schema.json"
      provides: "Enriched scan-input schema with 6 new field groups"
      contains: "catalyst_stack"
    - path: "assets/methodology/structured-analysis.schema.json"
      provides: "Enriched structured-analysis schema with matching fields"
      contains: "catalyst_stack"
    - path: "tests/test_schema_enrichment.py"
      provides: "Schema validation tests for all 6 new field groups"
      min_lines: 80
    - path: "src/edenfintech_scanner_bootstrap/pipeline.py"
      provides: "Pipeline validation gates for SCHM-07 and SCHM-08"
      contains: "_validate_catalyst_stack"
  key_links:
    - from: "assets/methodology/scan-input.schema.json"
      to: "src/edenfintech_scanner_bootstrap/schemas.py"
      via: "validate_instance loads schema and checks instances"
      pattern: "validate_instance.*_load_input_schema"
    - from: "src/edenfintech_scanner_bootstrap/pipeline.py"
      to: "assets/methodology/scan-input.schema.json"
      via: "validate_scan_input calls validate_instance with input schema"
      pattern: "validate_instance.*_load_input_schema"
    - from: "tests/fixtures/raw/merged_candidate_bundle.json"
      to: "assets/methodology/scan-input.schema.json"
      via: "fixtures must conform to updated schema for tests to pass"
---

<objective>
Enrich JSON schemas with 6 Codex field groups (catalyst_stack, invalidation_triggers, decision_memo, issues_and_fixes array, setup_pattern, stretch_case) and add two pipeline validation gates.

Purpose: The enriched schema is the contract that the Claude analyst agent (Phase 3) must satisfy. Pipeline gates enforce quality floors that prevent garbage-in-garbage-out.
Output: Updated schemas, pipeline gates, new tests, updated fixtures.
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/edenfintech_scanner_bootstrap/pipeline.py:
```python
def validate_scan_input(payload: dict) -> None:
    try:
        validate_instance(payload, _load_input_schema())
    except SchemaValidationError as exc:
        raise ValueError(f"scan_input schema validation failed: {exc}") from exc
    # ... then structural checks on scan_parameters, candidates
    # For candidates that pass screening:
    #   _require_keys(item, ["analysis", "epistemic_review"], ...)
    #   _base_case_details(item)
    #   _worst_case_details(item)
    #   _probability_details(item)
    #   _validate_pcs_answers(item)
```

From src/edenfintech_scanner_bootstrap/schemas.py:
```python
class SchemaValidationError(ValueError): ...
def validate_instance(instance: object, schema: dict, *, root_schema: dict | None = None, path: str = "$") -> None:
    # Handles $ref resolution for both #/$defs/ and #/definitions/ paths
```

CRITICAL schema convention difference (from research):
- scan-input.schema.json uses `$defs` (JSON Schema 2020-12 style)
- structured-analysis.schema.json uses `definitions` (draft-07 style)
Keep each file's existing convention.

Current issues_and_fixes in schemas: `{"type": "string"}` -- must be changed to array of objects.
This WILL break all fixtures. All fixtures must be updated in same task.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich schemas and write validation tests</name>
  <files>assets/methodology/scan-input.schema.json, assets/methodology/structured-analysis.schema.json, tests/test_schema_enrichment.py</files>
  <behavior>
    - test_catalyst_stack_validation: Valid HARD/MEDIUM/SOFT entries pass; missing type fails; empty array fails (minItems: 1)
    - test_invalidation_triggers: Valid {trigger, evidence} objects pass; missing fields fail
    - test_decision_memo: Valid {better_than_peer, safer_than_peer, what_makes_wrong} passes; missing field fails
    - test_issues_and_fixes_array: Valid array with evidence_status enum passes; invalid enum value fails; old string format fails
    - test_setup_pattern: Valid enum values pass (SOLVENCY_SCARE, QUALITY_FRANCHISE, NARRATIVE_DISCOUNT, NEW_OPERATOR, OTHER); invalid value fails
    - test_stretch_case: Valid object with same shape as base_case passes; missing required field fails
  </behavior>
  <action>
    Write tests/test_schema_enrichment.py FIRST with TestSchemaEnrichment class. Each test loads the real schema via schemas.validate_instance and tests both valid and invalid inputs. Use minimal valid scan-input payloads that include only the fields needed to test each new property.

    Then enrich both schemas:

    **scan-input.schema.json** (uses `$defs`):
    Add to `$defs.candidate.properties.analysis.properties`:
    - `catalyst_stack`: array of {type: enum HARD/MEDIUM/SOFT, description: string, timeline: string}, minItems: 1
    - `invalidation_triggers`: array of {trigger: string, evidence: string}, minItems: 1
    - `decision_memo`: object with required [better_than_peer, safer_than_peer, what_makes_wrong], all strings
    - `issues_and_fixes`: REPLACE existing string type with array of {issue: string, fix: string, evidence_status: enum ANNOUNCED_ONLY/ACTION_UNDERWAY/EARLY_RESULTS_VISIBLE/PROVEN}, minItems: 1
    - `setup_pattern`: string enum [SOLVENCY_SCARE, QUALITY_FRANCHISE, NARRATIVE_DISCOUNT, NEW_OPERATOR, OTHER]
    - `stretch_case`: object with same shape as existing base_case (revenue_b, fcf_margin_pct, multiple, shares_m, years required; discount_path optional)
    Add all 6 to the `required` array for analysis.

    **structured-analysis.schema.json** (uses `definitions`):
    Add matching fields to analysis_inputs section. Use same shapes. Match existing convention (`definitions` not `$defs`). Also add stretch_case_assumptions alongside base_case_assumptions using same shape.

    Run schema enrichment tests, iterate until green.
  </action>
  <verify>
    <automated>cd /home/laudes/zoot/projects/edenfintech-scanner-python && python -m unittest tests.test_schema_enrichment -v</automated>
  </verify>
  <done>Both schemas contain all 6 new field groups. Schema validation tests pass for valid and invalid inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pipeline gates and fixture updates</name>
  <files>src/edenfintech_scanner_bootstrap/pipeline.py, tests/test_scan_pipeline.py, tests/fixtures/raw/merged_candidate_bundle.json, tests/fixtures/raw/ranked_candidate_bundle.json, tests/fixtures/generated/merged_candidate_draft_overlay.json, assets/fixtures/regression/2026-03-07-cps-aap-dorm-pypl-scan-report-v5.json, assets/fixtures/regression/pending-human-review-exception.json, assets/fixtures/regression/manifest.json</files>
  <behavior>
    - test_rejects_no_hard_medium_catalysts: Candidate with catalyst_stack containing only SOFT entries raises ValueError
    - test_rejects_all_announced_only: Candidate where all issues_and_fixes have evidence_status ANNOUNCED_ONLY raises ValueError
    - test_passes_with_hard_catalyst: Candidate with at least one HARD catalyst passes gate
    - test_passes_with_mixed_evidence: Candidate with at least one non-ANNOUNCED_ONLY issue passes gate
  </behavior>
  <action>
    Write tests FIRST in tests/test_scan_pipeline.py, adding a TestPipelineGates class. Use minimal valid candidate dicts that pass screening (so the pipeline reaches analysis validation).

    Then implement in pipeline.py:
    1. Add `_validate_catalyst_stack(candidate, ticker)`: extract analysis.catalyst_stack, count HARD/MEDIUM entries, raise ValueError if zero
    2. Add `_validate_issues_and_fixes(candidate, ticker)`: extract analysis.issues_and_fixes, check if ALL are ANNOUNCED_ONLY, raise ValueError if so
    3. Call both from `validate_scan_input()` inside the `if screening_passed:` block, after existing checks

    Then update ALL fixtures to include the new schema fields:
    - tests/fixtures/raw/merged_candidate_bundle.json: Add catalyst_stack, invalidation_triggers, decision_memo, issues_and_fixes (array format), setup_pattern, stretch_case to each candidate's analysis section
    - tests/fixtures/raw/ranked_candidate_bundle.json: Same additions
    - tests/fixtures/generated/merged_candidate_draft_overlay.json: Add matching fields to overlay entries
    - assets/fixtures/regression/*.json: Add new fields to any candidate analysis sections

    Use realistic but minimal fixture data. Ensure at least one HARD or MEDIUM catalyst and at least one non-ANNOUNCED_ONLY issue per candidate (so they pass the new gates).

    Run full test suite + validate-assets + regression to confirm zero breakage.
  </action>
  <verify>
    <automated>cd /home/laudes/zoot/projects/edenfintech-scanner-python && python -m unittest discover -s tests -v && python -m edenfintech_scanner_bootstrap.cli validate-assets && python -m edenfintech_scanner_bootstrap.cli run-regression</automated>
  </verify>
  <done>Pipeline rejects zero-HARD/MEDIUM catalyst_stack and all-ANNOUNCED_ONLY issues_and_fixes. All fixtures updated. Full test suite + validate-assets + regression green.</done>
</task>

</tasks>

<verification>
1. `python -m unittest tests.test_schema_enrichment -v` -- schema enrichment tests pass
2. `python -m unittest tests.test_scan_pipeline -v` -- pipeline gate tests pass (including existing tests)
3. `python -m unittest discover -s tests -v` -- full suite green, no regressions
4. `python -m edenfintech_scanner_bootstrap.cli validate-assets` -- assets valid with enriched schemas
5. `python -m edenfintech_scanner_bootstrap.cli run-regression` -- regression fixtures pass
</verification>

<success_criteria>
- Both schemas contain catalyst_stack, invalidation_triggers, decision_memo, issues_and_fixes (array), setup_pattern, stretch_case as required fields
- Pipeline validate_scan_input rejects candidates with zero HARD/MEDIUM catalysts
- Pipeline validate_scan_input rejects candidates where all issues are ANNOUNCED_ONLY
- All existing tests, validate-assets, and regression suite pass with updated fixtures
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-02-SUMMARY.md`
</output>
