---
phase: 06-scan-modes-and-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/edenfintech_scanner_bootstrap/hardening.py
  - tests/test_hardening.py
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03]

must_haves:
  truths:
    - "CAGR exception panel runs 3 independent agent votes and requires unanimous approval"
    - "Non-unanimous CAGR exception stays in pending_review"
    - "Probability anchoring detection flags exactly 60% with friction risk type"
    - "Evidence quality scoring counts concrete vs vague citations and warns below threshold"
  artifacts:
    - path: "src/edenfintech_scanner_bootstrap/hardening.py"
      provides: "CAGR exception panel, anchoring detection, evidence quality scoring"
      exports: ["cagr_exception_panel", "detect_probability_anchoring", "score_evidence_quality", "ExceptionVote", "ExceptionPanelResult"]
    - path: "tests/test_hardening.py"
      provides: "Unit tests for all three hardening functions"
      min_lines: 120
  key_links:
    - from: "hardening.py:cagr_exception_panel"
      to: "analyst transport"
      via: "focused exception vote prompt via injected transport"
      pattern: "analyst_transport"
    - from: "hardening.py:cagr_exception_panel"
      to: "validator transport"
      via: "focused exception vote prompt via injected transport"
      pattern: "validator_transport"
    - from: "hardening.py:cagr_exception_panel"
      to: "epistemic transport"
      via: "focused exception vote prompt via injected transport"
      pattern: "epistemic_transport"
    - from: "hardening.py:score_evidence_quality"
      to: "epistemic_reviewer.is_weak_evidence"
      via: "reuses existing evidence quality detection"
      pattern: "is_weak_evidence"
---

<objective>
Build hardening gates: CAGR exception panel (3-agent vote), probability anchoring detection, and evidence quality scoring.

Purpose: Prevent unchecked LLM optimism by detecting bias patterns before overlays reach the deterministic pipeline.
Output: hardening.py module with three standalone functions, fully tested with mock transports.
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scan-modes-and-hardening/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/edenfintech_scanner_bootstrap/analyst.py:
```python
class ClaudeAnalystClient:
    def __init__(self, api_key: str, *, model: str = "claude-sonnet-4-5-20250514", transport: AnalystTransport | None = None):
    def analyze(self, raw_candidate: dict, *, sector_knowledge: dict | None = None, validator_objections: list[dict] | None = None) -> dict:
```

From src/edenfintech_scanner_bootstrap/validator.py:
```python
class RedTeamValidatorClient:
    def __init__(self, api_key: str, *, model: str = "claude-sonnet-4-5-20250514", transport=None):
    def validate(self, overlay_candidate: dict, raw_candidate: dict, contradictions: list[dict]) -> dict:
```

From src/edenfintech_scanner_bootstrap/epistemic_reviewer.py:
```python
CONCRETE_SOURCE_MARKERS = [
    "10-k", "10-q", "annual report", "earnings call", "sec filing",
    "q1", "q2", "q3", "q4", "fy20", "fy21", "fy22", "fy23", "fy24",
]

def is_weak_evidence(evidence_text: str) -> bool:

class EpistemicReviewerClient:
    def __init__(self, api_key: str, *, model: str = "claude-sonnet-4-5-20250514", transport=None):
    def review(self, review_input: EpistemicReviewInput) -> dict:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic hardening — probability anchoring and evidence quality scoring</name>
  <files>src/edenfintech_scanner_bootstrap/hardening.py, tests/test_hardening.py</files>
  <behavior>
    - Test: detect_probability_anchoring returns flag dict when base_probability_pct == 60.0 and dominant_risk_type is friction-carrying ("Cyclical/Macro", "Regulatory/Political", "Legal/Investigation", "Structural fragility (SPOF)")
    - Test: detect_probability_anchoring returns None for 60% with non-friction risk type
    - Test: detect_probability_anchoring returns None for non-60% probability
    - Test: score_evidence_quality counts concrete citations using CONCRETE_SOURCE_MARKERS from epistemic_reviewer
    - Test: score_evidence_quality returns methodology_warning when concrete_ratio below 0.5 threshold
    - Test: score_evidence_quality returns no warning when concrete_ratio at or above 0.5
    - Test: score_evidence_quality handles empty provenance gracefully (0 citations, no crash)
  </behavior>
  <action>
    Create src/edenfintech_scanner_bootstrap/hardening.py with two pure functions:

    1. detect_probability_anchoring(base_probability_pct: float, dominant_risk_type: str) -> dict | None
       - FRICTION_RISK_TYPES = {"Cyclical/Macro", "Regulatory/Political", "Legal/Investigation", "Structural fragility (SPOF)"}
       - If exactly 60.0 AND risk type in friction set: return {"flag": "PROBABILITY_ANCHORING_SUSPECT", "base_probability_pct": ..., "dominant_risk_type": ..., "reason": "..."}
       - Else: return None

    2. score_evidence_quality(overlay_candidate: dict, *, concrete_threshold: float = 0.5) -> dict
       - Import is_weak_evidence and CONCRETE_SOURCE_MARKERS from epistemic_reviewer
       - Iterate overlay_candidate["provenance"] entries, check each entry's "review_note" (the citation text)
       - Count concrete (any CONCRETE_SOURCE_MARKERS in lowered text) vs vague (is_weak_evidence returns True)
       - Compute concrete_ratio = concrete_count / total_citations (0.0 if no citations)
       - If concrete_ratio < concrete_threshold: set methodology_warning string
       - Return {"total_citations": int, "concrete_count": int, "vague_count": int, "concrete_ratio": float, "methodology_warning": str | None}

    Create tests/test_hardening.py with TestProbabilityAnchoring and TestEvidenceQuality classes.
    Use realistic overlay fixture dicts with provenance entries containing review_note fields.
  </action>
  <verify>
    <automated>cd /home/laudes/zoot/projects/edenfintech-scanner-python && python -m unittest tests.test_hardening.TestProbabilityAnchoring tests.test_hardening.TestEvidenceQuality -v</automated>
  </verify>
  <done>detect_probability_anchoring flags exactly 60% + friction risk type, score_evidence_quality counts concrete vs vague citations with warning below threshold, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CAGR exception panel with 3-agent unanimous vote</name>
  <files>src/edenfintech_scanner_bootstrap/hardening.py, tests/test_hardening.py</files>
  <behavior>
    - Test: cagr_exception_panel with all 3 agents approving returns approved=True, unanimous=True
    - Test: cagr_exception_panel with 1 agent rejecting returns approved=False, unanimous=False
    - Test: cagr_exception_panel with all agents rejecting returns approved=False, unanimous=True
    - Test: Each ExceptionVote contains agent name, approve bool, and reasoning string
    - Test: ExceptionPanelResult.votes has exactly 3 entries
  </behavior>
  <action>
    Add to hardening.py:

    1. ExceptionVote dataclass (frozen=True): agent: str, approve: bool, reasoning: str
    2. ExceptionPanelResult dataclass (frozen=True): votes: list[ExceptionVote], unanimous: bool, approved: bool

    3. cagr_exception_panel function:
       ```
       def cagr_exception_panel(
           overlay_candidate: dict,
           raw_candidate: dict,
           *,
           analyst_transport=None,
           validator_transport=None,
           epistemic_transport=None,
           config: AppConfig | None = None,
       ) -> ExceptionPanelResult:
       ```
       - Uses transport injection pattern (same as analyst/validator/epistemic clients) for testability
       - Creates focused short prompts for each agent asking: "This candidate has a {cagr}% base CAGR (20-29.9% exception range). Given the evidence summary below, should this candidate be granted an exception? Vote APPROVE or REJECT with reasoning."
       - Each agent gets: ticker, thesis_summary, catalyst_stack, key_risks, base_case_assumptions (summarized from overlay_candidate)
       - Each transport call returns JSON with {"approve": bool, "reasoning": str}
       - Collects 3 ExceptionVote objects
       - approved = all 3 approve; unanimous = all same vote
       - Returns ExceptionPanelResult

    Tests use mock transports that return canned {"approve": true/false, "reasoning": "..."} responses.
    Do NOT use the full ClaudeAnalystClient/RedTeamValidatorClient/EpistemicReviewerClient classes -- use lightweight direct transport calls with focused prompts to avoid the heavy analysis cost per research pitfall 4.
  </action>
  <verify>
    <automated>cd /home/laudes/zoot/projects/edenfintech-scanner-python && python -m unittest tests.test_hardening.TestCagrExceptionPanel -v</automated>
  </verify>
  <done>CAGR exception panel runs 3 agent votes via transport injection, unanimous approval required, non-unanimous stays pending, all tests pass</done>
</task>

</tasks>

<verification>
cd /home/laudes/zoot/projects/edenfintech-scanner-python && python -m unittest tests.test_hardening -v
</verification>

<success_criteria>
- detect_probability_anchoring correctly flags exactly 60% + friction risk type as PROBABILITY_ANCHORING_SUSPECT
- score_evidence_quality counts concrete vs vague citations and adds methodology warning below 50% threshold
- cagr_exception_panel runs 3 independent votes, requires unanimous approval, returns structured ExceptionPanelResult
- All tests pass, full suite has no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-scan-modes-and-hardening/06-01-SUMMARY.md`
</output>
