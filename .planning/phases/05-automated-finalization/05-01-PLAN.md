---
phase: 05-automated-finalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/edenfintech_scanner_bootstrap/structured_analysis.py
  - src/edenfintech_scanner_bootstrap/analyst.py
  - tests/test_structured_analysis.py
  - tests/test_analyst.py
autonomous: true
requirements: [AUTO-02, AUTO-03, AUTO-04]

must_haves:
  truths:
    - "LLM_CONFIRMED is accepted as a valid final provenance status by finalize_structured_analysis()"
    - "LLM_EDITED is accepted as a valid final provenance status by finalize_structured_analysis()"
    - "finalize_structured_analysis() converts LLM_DRAFT entries to LLM_CONFIRMED"
    - "reviewer='llm:model-id' format is accepted without error"
    - "Analyst re-runs with validator objections injected into the user prompt"
  artifacts:
    - path: "src/edenfintech_scanner_bootstrap/structured_analysis.py"
      provides: "Extended FINAL_PROVENANCE_STATUSES with LLM_CONFIRMED and LLM_EDITED"
      contains: "LLM_CONFIRMED"
    - path: "src/edenfintech_scanner_bootstrap/analyst.py"
      provides: "Objection injection in analyst prompt"
      contains: "validator_objections"
  key_links:
    - from: "structured_analysis.py"
      to: "finalize_structured_analysis()"
      via: "FINAL_PROVENANCE_STATUSES set membership"
      pattern: "LLM_CONFIRMED|LLM_EDITED"
    - from: "analyst.py"
      to: "_build_user_prompt()"
      via: "validator_objections parameter"
      pattern: "validator_objections"
---

<objective>
Extend provenance lifecycle for LLM-automated finalization and add validator objection injection to the analyst.

Purpose: AUTO-03/AUTO-04 are prerequisites for the automation orchestrator (Plan 02). AUTO-02 requires the analyst to accept objections for retry runs.
Output: Updated structured_analysis.py with LLM_CONFIRMED and LLM_EDITED statuses, updated analyst.py with objection injection, and tests for both.
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-automated-finalization/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/edenfintech_scanner_bootstrap/structured_analysis.py:
```python
FINAL_PROVENANCE_STATUSES = {"HUMAN_EDITED", "HUMAN_CONFIRMED"}
DRAFT_PROVENANCE_STATUSES = {"MACHINE_DRAFT", "LLM_DRAFT"}

def finalize_structured_analysis(
    payload: dict,
    *,
    reviewer: str,
    final_status: str = "HUMAN_CONFIRMED",
    note: str | None = None,
) -> dict:
```

From src/edenfintech_scanner_bootstrap/analyst.py:
```python
def _build_user_prompt(raw_candidate: dict, evidence_context: dict, evidence_snippets: set[str]) -> str:

class ClaudeAnalystClient:
    def analyze(
        self,
        raw_candidate: dict,
        *,
        sector_knowledge: dict | None = None,
    ) -> dict:

def generate_llm_analysis_draft(
    raw_bundle: dict,
    *,
    client: ClaudeAnalystClient,
    sector_knowledge: dict | None = None,
) -> dict:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend provenance statuses and finalization for LLM automation</name>
  <files>src/edenfintech_scanner_bootstrap/structured_analysis.py, tests/test_structured_analysis.py</files>
  <behavior>
    - Test: LLM_CONFIRMED is in FINAL_PROVENANCE_STATUSES set
    - Test: LLM_EDITED is in FINAL_PROVENANCE_STATUSES set
    - Test: finalize_structured_analysis() with final_status="LLM_CONFIRMED" converts LLM_DRAFT entries to LLM_CONFIRMED
    - Test: finalize_structured_analysis() with final_status="LLM_EDITED" converts LLM_DRAFT entries to LLM_EDITED
    - Test: finalize_structured_analysis() with reviewer="llm:claude-sonnet-4-5-20250514" succeeds (non-empty string check unchanged)
    - Test: apply_structured_analysis() accepts overlay with LLM_CONFIRMED provenance (not rejected as draft)
    - Test: apply_structured_analysis() accepts overlay with LLM_EDITED provenance (not rejected as draft)
    - Test: review_structured_analysis() counts LLM_CONFIRMED and LLM_EDITED entries correctly (add to counts)
  </behavior>
  <action>
    1. In structured_analysis.py, add "LLM_CONFIRMED" and "LLM_EDITED" to FINAL_PROVENANCE_STATUSES set (line 49). Result: FINAL_PROVENANCE_STATUSES = {"HUMAN_EDITED", "HUMAN_CONFIRMED", "LLM_CONFIRMED", "LLM_EDITED"}
    2. In review_structured_analysis(), add counters for "llm_confirmed" and "llm_edited" alongside existing human_confirmed/human_edited counters. Add both keys to total_counts dict initialization. Add elif branches for LLM_CONFIRMED and LLM_EDITED status counting.
    3. In tests/test_structured_analysis.py, add new test class TestLLMFinalization with tests for: LLM_CONFIRMED accepted as final_status, LLM_EDITED accepted as final_status, LLM_DRAFT->LLM_CONFIRMED transition works, LLM_DRAFT->LLM_EDITED transition works, reviewer="llm:model-id" format accepted, finalized overlay passes apply_structured_analysis validation for both statuses. Use existing test fixtures pattern from the file.

    Note: finalize_structured_analysis() already accepts any non-empty string for reviewer and uses set membership for final_status. Only the set needs updating. The reviewer format "llm:<model-id>" requires no code change beyond the status set. LLM_EDITED covers the case where an LLM modifies an existing overlay (distinct from LLM_CONFIRMED which is a direct promotion of LLM_DRAFT).
  </action>
  <verify>
    <automated>cd /home/laudes/zoot/projects/edenfintech-scanner-python && python -m unittest tests.test_structured_analysis -v 2>&1 | tail -20</automated>
  </verify>
  <done>LLM_CONFIRMED and LLM_EDITED in FINAL_PROVENANCE_STATUSES, finalize accepts both as final_status with reviewer="llm:*", review counts both entry types, all existing tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add validator objection injection to analyst prompt and analyze method</name>
  <files>src/edenfintech_scanner_bootstrap/analyst.py, tests/test_analyst.py</files>
  <behavior>
    - Test: _build_user_prompt with validator_objections=None produces identical output to current behavior
    - Test: _build_user_prompt with validator_objections=[{"question": "...", "objection": "..."}] appends "VALIDATOR OBJECTIONS" section to prompt
    - Test: ClaudeAnalystClient.analyze() accepts validator_objections kwarg and passes to _build_user_prompt
    - Test: generate_llm_analysis_draft() accepts validator_objections kwarg and passes to client.analyze()
    - Test: Objection text appears in the prompt sent to transport (verify via mock transport inspection)
  </behavior>
  <action>
    1. In analyst.py, extend _build_user_prompt signature to accept validator_objections: list[dict] | None = None. When not None, append section:
       - Empty line
       - "VALIDATOR OBJECTIONS (from previous attempt -- you MUST address these):"
       - json.dumps(validator_objections, indent=2)
       - Empty line
       - "Revise your analysis to address each objection explicitly."

    2. In ClaudeAnalystClient.analyze(), add validator_objections: list[dict] | None = None parameter. Pass through to _build_user_prompt call.

    3. In generate_llm_analysis_draft(), add validator_objections: list[dict] | None = None parameter. Pass through to client.analyze() call.

    4. In tests/test_analyst.py, add TestValidatorObjectionInjection class with tests for: None produces no objection section, list produces section with content, transport receives prompt containing objections, generate_llm_analysis_draft passes through correctly.
  </action>
  <verify>
    <automated>cd /home/laudes/zoot/projects/edenfintech-scanner-python && python -m unittest tests.test_analyst -v 2>&1 | tail -20</automated>
  </verify>
  <done>Analyst accepts validator_objections parameter at all three levels (_build_user_prompt, analyze, generate_llm_analysis_draft), objections appear in LLM prompt, None produces unchanged behavior, all existing tests pass</done>
</task>

</tasks>

<verification>
cd /home/laudes/zoot/projects/edenfintech-scanner-python && python -m unittest tests.test_structured_analysis tests.test_analyst -v
</verification>

<success_criteria>
- FINAL_PROVENANCE_STATUSES contains both LLM_CONFIRMED and LLM_EDITED
- finalize_structured_analysis(final_status="LLM_CONFIRMED", reviewer="llm:model-id") succeeds
- finalize_structured_analysis(final_status="LLM_EDITED", reviewer="llm:model-id") succeeds
- generate_llm_analysis_draft(validator_objections=[...]) injects objections into analyst prompt
- All existing tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/05-automated-finalization/05-01-SUMMARY.md`
</output>
